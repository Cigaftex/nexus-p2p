# Nexus Flutter app

The Flutter shell renders nearby peers, pairing, direct chat, and file sending.
All identity, encryption, storage, transfer, and sync operations cross the C FFI
into `core/nexus-core`. See the repository root README for build and run steps.
